numpy>=1.23,<2
opencv-python>=4.8,<5
pyserial>=3.5,<4
mfrc522>=0.0.7
RPi.GPIO>=0.7.1
spidev>=3.6

# MediaPipe on Pi can differ by architecture.
# Pi 5 aarch64 should use the official MediaPipe wheel.
mediapipe==0.10.33; platform_machine == "aarch64"
mediapipe-rpi4==0.8.8; platform_machine == "armv7l"
